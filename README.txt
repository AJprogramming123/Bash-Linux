git status
git add .
git commit -m "Your commit message"
git pull origin your-branch-name --rebase     # Git will fetch these latest changes that were made by others to the remote repository, and apply them to your local branch
git push origin your-branch-name
